export async function extractTextFromImage(_uri: string): Promise<string> {
  throw new Error(
    "Photo scanning now uses the online syllabus parser so the iOS app stays compatible with native simulator and WidgetKit builds. Paste text or upload a text-based PDF if the online parser is unavailable."
  );
}
